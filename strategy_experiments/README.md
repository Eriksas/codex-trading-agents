# Strategy Experiments

Hermes experiment mode writes draft JSON files under dated subdirectories:

```text
strategy_experiments/YYYY-MM-DD/hermes_experiments.json
```

These files are generated artifacts and may contain personal research context, so they are ignored by default. Promote an experiment into `strategy.json` only after manual review and backtest validation.
